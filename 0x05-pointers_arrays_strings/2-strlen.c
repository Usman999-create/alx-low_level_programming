#include <stdio.h>
#include "main.h"
/**
 * _strlen - function
 * @s: parameter s
 *
 * Return: return value n
 */
int _strlen(char *s)
{
	int n;


	for (s[n] = 'n'; s[n] != '\0'; n++)
		return (n);
}
