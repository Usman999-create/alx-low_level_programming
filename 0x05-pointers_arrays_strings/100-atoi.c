#include "main.h"
#include <stdio.h>
/**
 * _atoi - function that convert string to numbers
 * @s: parameter
 *
 * Return: Always 0.
 */
int _atoi(char *s)
{
	char s[] = "123";
	int a = atoi(s);


	printf("%d", a);
	return (0);
}
