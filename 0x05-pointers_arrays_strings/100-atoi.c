#include "main.h"
#include <stdio.h>
/**
 * _atoi - function that convert string to numbers
 * @s: parameter
 *
 * Return: Always 0.
 */
int _atoi(char *s)
{
	char ch[] = "123";
	int a = _atoi(ch);


	printf("%d", a);
	return (0);
}
