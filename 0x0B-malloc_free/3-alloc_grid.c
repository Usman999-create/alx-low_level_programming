#include "main.h"
#include <stdio.h>
/**
 * **alloc_grid - create a two dimensional array
 * if ints
 * @width: width of matrix
 * @height: height of matrix
 *
 * Return: pointer of the created matrix (success)
 */
int **alloc_grid(int width, int height)
{
	int x;
