#include <stdio.h>
/**
 * main - Entry point
 * Description: prints all the alphabets in lower case
 *
 * Return: return all the alphabets
 */
int main(void)
{
	int i = '0';

	for (i = 0; i <= 10; i++)
	{
		putchar("%d", i);
	{
	putchar(',');
	putchar('');
	}
	putchar("\n");
	return (0);
	}
}
