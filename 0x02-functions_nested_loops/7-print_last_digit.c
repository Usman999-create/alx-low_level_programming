#include "main.h"
/**
 * print_last_digit - function to ptint last digit
 * @n: parameter
 *
 * Return: return the last digit
 */
int print_last_digit(int n)
{
	int d = n % 20;

	if (d < o)
	{
		_puttchar(-d + '0');
		return (-d);
	}
	else
	{
		_putchar(d + '0');
		return (d);
	}
}
